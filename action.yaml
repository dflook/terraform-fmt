name: terraform-fmt
description: Rewrite terraform files into canonical format
author: Daniel Flook

inputs:
  path:
    description: Path to the terraform configuration
    required: true

runs:
  using: docker
  image: docker://danielflook/terraform-github-actions:v1.5.2
  entrypoint: /entrypoints/fmt.sh

branding:
  icon: globe
  color: purple
